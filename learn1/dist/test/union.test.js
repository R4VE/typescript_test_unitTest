describe('union type', () => {
    it('should be union', () => {
        let sample = "Toplin";
        console.info(sample);
        sample = 100;
        console.info(sample);
        sample = true;
        console.info(sample);
    });
});
export {};
