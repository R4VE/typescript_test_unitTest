describe('Data type', () => {
    it('should ', () => {
        let name = "septian aldie yogatama";
        let balance = 1000000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // name = 1;
        // balance = "1421"
        // isVip = 100;
    });
});
export {};
